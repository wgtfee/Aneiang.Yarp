# V0.7 Stage Report

Status: PASS for the Gateway scope delivered in this repository.

## G00-G17

| Stage | Result | Evidence |
|---|---|---|
| G00-G03 | PASS | Gateway contract and independent frontend/backend health model documented. |
| G04-G05 | PASS | YARP active/passive checks, routes, clusters and destinations configured. |
| G06-G08 | PASS | `/mes`, `/iot`, `/wcs`, `/thinggateway` frontend routes and API routes; multi-destination model retained. |
| G09-G10 | PASS | Health summary, snapshot and VOL.web Gateway Health page. |
| G11 | PASS | `/{prefix}/hubs/health` with IAM bearer/query-token support. |
| G12 | PASS | SQLite migration 010, history API and retention cleanup. |
| G13 | PASS | ThingGetWay is marked `healthRole=device-gateway`; device state remains outside Gateway health. |
| G14 | PASS | Smoke script covers live/ready/healthz, cluster inventory, summary, snapshot and history. |
| G15 | PASS | Blue/green switch API and dashboard operation are available for clusters with blue/green destinations. |
| G16 | PASS | Gateway smoke test passed with five configured clusters. Business/device processes were not started automatically. |
| G17 | PASS | Delivery and rollback instructions are in `V0.7-HEALTH-DELIVERY.md`. |

## Verification

- `dotnet build Aneiang.Yarp.sln --no-restore`: PASS, 0 errors.
- `npm run build` in `vol.web`: PASS.
- `tests/V07-HealthSmoke.Tests.ps1 -RequireDashboardApi`: PASS.
- Smoke verification found: `iam`, `mes`, `iot`, `wcs`, `thinggateway`.

## Rollback

Remove or disable the `thinggateway-api`, `thinggateway-frontend` routes and `thinggateway` cluster in the Gateway configuration. Existing business routes remain independent. Health history is stored only in Gateway SQLite storage.

## External prerequisite

ThingGetWay's repository requires its private/local `nupkgs` source for a full restore. The Gateway integration and health contract are committed in source; run ThingGetWay with its normal repository restore/package setup before enabling live device collection.
